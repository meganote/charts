extraArgs:
  - --enable-skip-login

metricsScraper:
  enabled: true

metrics-server:
  enabled: true
  args:
    - --kubelet-insecure-tls

ingress:
  enabled: true

#  annotations:
#    kubernetes.io/ingress.class: nginx

  className: "nginx"

  paths:
    - /

  hosts:
    - dashboard.k8s-master
